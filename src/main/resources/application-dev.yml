spring:
  application:
    name: online-store-spring
  datasource:
    url: jdbc:mysql://localhost:3306/online_store
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 10MB

server:
  port: 8082

student:
  portal:
    upload:
      image:
        directory:
          path: /Users/rt/Desktop/Java/Spring/online-store-spring/uploaded_images